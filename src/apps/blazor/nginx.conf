events { }

http {
    include mime.types;

    types {
        application/wasm wasm;
    }

    server {
        listen 7100;
        index index.html;

        location / {
            root /usr/share/nginx/html;
            try_files $uri $uri/ /index.html =404;
        }
    }
}
